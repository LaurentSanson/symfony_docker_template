version: "3.8"
services:

  mysql:
    image: mysql
    container_name: mysql_docker
    command: mysqld --character-set-server=utf8 --collation-server=utf8_unicode_ci --init-connect='SET NAMES UTF8;' --innodb-flush-log-at-trx-commit=0
    volumes:
      - db-data:/var/lib/mysql
    ports:
      - 3306:3306
    environment:
      MYSQL_DATABASE: "exemple"
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"

  maildev:
    image: maildev/maildev
    container_name: maildev_docker
    command: bin/maildev --web 80 --smtp 25 --hide-extensions STARTTLS
    ports:
      - "8081:80"

  php:
    build:
      context: .
    container_name: php_docker
#    command: bash -c './bin/console server:run 0.0.0.0'
    volumes:
      - .:/var/www
    ports:
      - 8000:8000
    environment:
      DATABASE_URL: mysql://root:@database:3306/exemple?serverVersion=${MYSQL_VERSION:-13}

#  node:
#    image: node:latest
#    environment:
#      - NODE_ENV=production
#    volumes:
#      - ./:/var/www/mypack
#    expose:
#      - "8082"
#    command: bash -c "yarn --production=false && yarn run watch"
#    depends_on:
#      - php

volumes:
  db-data: